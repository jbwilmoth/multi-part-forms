<div class="container">
  <% @users.each do |u| %>
    <div class="user-display">
      <img src="<%= u.photo %>" title="<%= u.name %>">
      <p><%= u.name %></p>
    </div>
  <% end %>
  <br>
  <a href="/users/sign_up">Add User</a>
</div>
